# Concept gallery — preview only

All four portfolio concepts, built and running side by side so they can be compared at a single
URL. **This branch is not for merging** — it would ship four portfolios to the homepage.

| Path | Concept | Idea |
|---|---|---|
| `/source/` | **SOURCE** | The portfolio *is* a design system — live tokens, component inspector, chaos button. **Proposed for production.** |
| `/orchestrator/` | **ORCHESTRATOR** | State an intent; a real dependency graph fans out agents and the page assembles itself. |
| `/signal/` | **SIGNAL** | Editorial restraint with one velocity-reactive particle field. |
| `/clearance/` | **CLEARANCE** | Badge in, doors unlock, career rendered as an audit log. Parked. |

The production candidate lives on `redesign/source` (PR #10). Each concept is also on its own
branch in the `Portfolio` concept-lab repo.

All four still show amber `[XX]` placeholders where real numbers belong.
